# 💰 Система расчета заработной платы (Payroll System)

## 🌟 Обзор проекта

Это настольное WPF-приложение на C# (.NET), разработанное для управления видами работ, учета рабочего времени сотрудников и **автоматического расчета заработной платы**.

Ключевой особенностью является использование **паттерна "Стратегия"** для гибкого применения различных правил начисления зарплаты (почасовая ставка, почасовая ставка с бонусом и т.д.).

### Основные функции

* **Управление сотрудниками:** Добавление, редактирование и удаление сотрудников.
* **Управление работами:** Создание и настройка видов работ с различными ставками и стратегиями начисления.
* **Назначение работ:** Прикрепление выполненных часов к сотруднику.
* **Автоматический расчет:** Мгновенный пересчет общей зарплаты сотрудника на основе примененных стратегий.
* **Персистенция данных:** Надежное сохранение всех данных в локальной базе данных.

## 🚀 Технологический стек

| Категория | Технология | Описание |
| :--- | :--- | :--- |
| **Интерфейс** | C# / WPF (.NET) | Основной язык и фреймворк для разработки настольного приложения. |
| **База данных** | SQLite | Легковесная, бессерверная база данных для локального хранения данных. |
| **ORM** | Entity Framework Core (EF Core) | Используется для взаимодействия с базой данных (CRUD-операции). |
| **Шаблон проектирования** | Паттерн "Стратегия" | Применен для инкапсуляции алгоритмов расчета зарплаты. |

---

## 💡 Архитектура и Решение ключевых проблем

### 1. Паттерн "Стратегия"

Система использует Стратегию для определения алгоритма начисления зарплаты для каждого вида работы:

* **Интерфейс:** `ISalaryStrategy`
* **Реализации:** `HourlyStrategy` (простая почасовая ставка), `BonusPercentStrategy` (ставка с дополнительным процентом).


Класс `Work` хранит только данные, необходимые для **реконструкции** выбранной стратегии (`StrategyTypeKey`, `BonusPercentValue`).

### 2. Структура данных (Классы и связи)

| Класс | Описание | Основные связи |
| :--- | :--- | :--- |
| **`Employee`** | Сотрудник. Хранит ФИО и должность. | ➡️ `ICollection<CompletedWork>` (Один-ко-Многим) |
| **`Work`** | Вид работы. Хранит ставку и ключи стратегии. | ➡️ `ICollection<CompletedWork>` (Один-ко-Многим) |
| **`CompletedWork`** | Выполненная работа. Учет часов. | Внешние ключи к `Employee` и `Work`. |

### 3. Решение проблемы с обнулением зарплаты при перезапуске (Persistence Challenge)

Для обеспечения корректного отображения вычисленной зарплаты (`TotalSalary`) после загрузки из БД были применены следующие техники:

1.  **Глубокая загрузка (Eager Loading):** Использование `Include()` и `ThenInclude()` в EF Core для гарантированной загрузки всех необходимых связанных объектов (`Employee` → `CompletedWorks` → `WorkItem`).
2.  **Принудительная инициализация стратегии:** Свойство `TotalSalary` является вычисляемым и зависит от создания временного объекта стратегии (`WorkItem.GetStrategy()`). В методе `LoadDataFromDatabase()` добавлен цикл, который принудительно вызывает `completedWork.CalculateCost()` для каждой записи, заставляя систему создать и инициализировать стратегию сразу после загрузки.

---

## 🛠️ Как запустить проект

1.  **Клонируйте** репозиторий:
    ```bash
    git clone [https://github.com/Formox/Payroll_system]
    ```
2.  **Откройте** решение (`.sln`) в Visual Studio.
3.  **Запустите** проект (F5).
4.  При первом запуске база данных `payroll.db` будет автоматически создана в папке `bin/Debug` и заполнена дефолтными данными.

---

## 📄 Основные файлы

| Файл | Назначение |
| :--- | :--- |
| `MainWindow.xaml.cs` | Главная логика: управление UI, загрузка/сохранение в БД, сводные расчеты. |
| `Payroll.cs` | Все модели данных (`Employee`, `Work`) и реализации стратегий расчета зарплаты. |
| `PayrollContext.cs` | Контекст базы данных для EF Core и настройка связей. |
| `*Window.xaml.cs` | Логика вспомогательных окон (добавление/редактирование). |
